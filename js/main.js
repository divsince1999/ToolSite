// Global JavaScript for the ToolBox platform
document.addEventListener('DOMContentLoaded', () => {
    console.log('ToolBox Platform Initialized');

    // Future global features can be added here
    // e.g., Theme switching, mobile menu toggle, analytics, etc.
});
